# Launch Screen Assets

These images are the CareBridge mark on a transparent background, shown on
the board's background color until Flutter draws its first frame. They are
generated by `tool/branding/generate_app_icons.py`; edit that script rather
than these files.
